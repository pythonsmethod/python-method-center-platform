name: Application security and regression
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run security:check
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm audit
      - run: npm run build
      - run: git diff --check
