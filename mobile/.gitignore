# Expo build and dev artefacts
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are generated by `expo prebuild`; app.json is the source
# of truth for identifiers, permissions and schemes.
/ios/
/android/

# Local secrets — mobile/.env.example lists the public values only.
.env
.env.local
.env.*.local

*.log
*.tsbuildinfo
