# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are generated by `expo prebuild` and are not committed:
# app.json is the source of truth for identifiers, permissions and schemes.
/ios/
/android/

# Local secrets — mobile/.env.example lists the three public values.
.env
.env.local
.env.*.local

*.log
*.tsbuildinfo
