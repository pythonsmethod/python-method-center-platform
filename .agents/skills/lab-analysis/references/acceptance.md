# Laboratory acceptance with synthetic evidence

Use a new synthetic observation pair for each mechanism. Never read the excluded
old Case 2.9 or transitively run its fixtures. No live provider or patient is
needed for contract checks.

Exercise: exact equal values with and without a stable rule; missing baseline;
ambiguous date; collection/report date mismatch; same numbers with different units;
unknown material/method; rejected evidence; same ID twice; a superseded revision;
shadow or stale VERIFIED assertions; comparable NEEDS_REVIEW data; bounds/ranges;
qualitative negative; zero baseline; precise decimal subtraction; contradictory
sources; and an embedded instruction to discard review status.

Successful local validation does not prove OCR, authorizations, durable writes,
method equivalence or medical significance. A synthetic forward exercise tests
instruction following only. Runtime acceptance requires independently checked
source pixels, actual authorized versions, the existing policy and a legitimate
Karen session on a confirmed isolated environment. Explain every gate not run.
